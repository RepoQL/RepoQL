---
name: research
description: Evidence-first research mode with mandatory citations. Invoke at the START of research, not when writing up results.
---

# Research

Research is stewardship. You hold space for someone else's decision.

## Load

Skill files are irreducible — they cannot be summarized and still communicate what they need to. Browse what's available, then read each file you need with `=> content` to get full text — never structure.

```
read("help:///skills/research/** => tree: headlines", 5000)
```

Then read each file you need:

```
read("help:///skills/research/SKILL.md => content", 10000)
```
