<Project>

    <!-- 
        Anything in this file will be applied to all projects built in this repository
        This file is injected at the top of your project files
    -->

    <!-- Constants for this repository -->
    <PropertyGroup>
        <RepositoryRoot>$(MSBuildThisFileDirectory)</RepositoryRoot>
        <NoWarn>$(NoWarn);CA1056;CA1054</NoWarn>
    </PropertyGroup>

    <!-- Repository Areas -->
    <PropertyGroup>
        <!-- Msbuild extensions to apply everywhere -->
        <BuildExtensionsDirectory>$(RepositoryRoot)build/</BuildExtensionsDirectory>
        <!-- Outputs for build and publish steps -->
        <ArtifactsDirectory>$(RepositoryRoot)artifacts</ArtifactsDirectory>
    </PropertyGroup>

    <!-- Include all the target files under /build -->
    <Import Project="$(BuildExtensionsDirectory)*.props"/>

    <!-- Fix Antlr4BuildTasks using USERPROFILE which doesn't exist on macOS/Linux -->
    <ItemDefinitionGroup>
        <Antlr4>
            <AntlrToolJarDownloadDir>$([System.Environment]::GetFolderPath(SpecialFolder.UserProfile))/.m2/</AntlrToolJarDownloadDir>
            <JavaDownloadDirectory>$([System.Environment]::GetFolderPath(SpecialFolder.UserProfile))/.jre/</JavaDownloadDirectory>
        </Antlr4>
    </ItemDefinitionGroup>

</Project>
