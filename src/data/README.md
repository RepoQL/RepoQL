# data/

The graph store and vector layer. Depends on contracts + infra.

Put it here if it persists or retrieves graph data — DuckDB access, UDFs, schema, snapshots, local ONNX embeddings. The single writer constraint lives here.

Projects: `Data.DuckDB`, `Embeddings`.
