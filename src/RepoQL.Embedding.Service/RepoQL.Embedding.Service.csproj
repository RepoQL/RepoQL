<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <AssemblyName>repoql-embedding</AssemblyName>

    <!-- dotnet publish produces a container image by default -->
    <PublishProfile>DefaultContainer</PublishProfile>
    <ContainerRepository>repoql-embedding</ContainerRepository>
    <ContainerFamily>noble</ContainerFamily>
    <UserSecretsId>dbcf7209-9611-44dd-924e-b35e955f586a</UserSecretsId>
  </PropertyGroup>

  <ItemGroup>
    <ContainerPort Include="8080" Type="tcp" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Grpc.AspNetCore" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\RepoQL.Embedding.Proto\RepoQL.Embedding.Proto.csproj" />
  </ItemGroup>

</Project>
