# cloud/

Independently deployed remote services. Self-contained.

Put it here if it runs as its own Cloud Run service — embedding generation, LLM inference, auth primitives. These have their own proto definitions, clients, and service implementations. They don't depend on the host or the pipeline.

Projects: `Cloud.Auth`, `Cloud.Service`, `Embedding.*`, `Inference.*`.
