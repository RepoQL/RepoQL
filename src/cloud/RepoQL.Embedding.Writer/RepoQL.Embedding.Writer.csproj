<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AssemblyName>repoql-embedding-writer</AssemblyName>

    <!-- dotnet publish produces a container image by default -->
    <PublishProfile>DefaultContainer</PublishProfile>
    <ContainerRepository>repoql-embedding-writer</ContainerRepository>
    <ContainerFamily>noble</ContainerFamily>
    <PublishTrimmed>false</PublishTrimmed>
    <UserSecretsId>31e51d7c-0e21-4a78-a55f-505493414b2f</UserSecretsId>
  </PropertyGroup>

  <ItemGroup>
    <ContainerPort Include="8080" Type="tcp" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="AWSSDK.S3" />
    <PackageReference Include="DuckDB.NET.Data.Full" />
    <PackageReference Include="Google.Cloud.Storage.V1" />
    <PackageReference Include="Google.Cloud.Tasks.V2" />
    <PackageReference Include="OpenTelemetry" />
    <PackageReference Include="OpenTelemetry.Exporter.OpenTelemetryProtocol" />
    <PackageReference Include="OpenTelemetry.Extensions.Hosting" />
    <PackageReference Include="OpenTelemetry.Instrumentation.AspNetCore" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\RepoQL.Embedding.Storage\RepoQL.Embedding.Storage.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="RepoQL.Embedding.Writer.Tests" />
  </ItemGroup>

</Project>
