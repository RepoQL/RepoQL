# contracts/

Pure types and interfaces. Depends on nothing internal.

Put it here if it's a shared type, interface, enum, or model that other projects depend on. No implementations — only definitions.

Examples: `RepoUri`, `SemanticMediaType`, `TokenEstimator`, `IInferenceProvider`, `Edge`, `Node`, `Annotation`, `IGraphQueryService`.
