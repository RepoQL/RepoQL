# tests/

Shared test infrastructure. Most test projects live next to their source.

This folder contains only `RepoQL.Testing` (shared helpers, scaffolding, format test base classes) and `Directory.Build.props` (test settings inherited by all test projects via import). Individual test projects live alongside the code they test in each tier.
