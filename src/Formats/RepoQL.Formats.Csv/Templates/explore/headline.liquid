{{ file_name }} | {{ media_kind }} | {{ size_bytes | filesize }}{% if token_count > 0 %}, {{ token_count | tokens }}{% endif %} | {{ row_count | number_with_delimiter }} rows, {{ column_count | number_with_delimiter }} cols{% if column_names and column_names.size > 0 %}: {% for name in column_names limit:4 %}{{ name }}{% unless forloop.last %}, {% endunless %}{% endfor %}{% if column_names.size > 4 %}, ...{% endif %}{% endif %}
