{{ file_name }} | {% if primary_object %}{{ primary_object.type }} {{ primary_object.name }}{% if primary_object.return_type %} → {{ primary_object.return_type }}{% endif %}{% else %}sql{% endif %}{% if indexes.size > 0 %} | +{{ indexes.size }} index{% if indexes.size > 1 %}es{% endif %}{% endif %}{% if has_multiple_objects and tables.size > 1 %} | {{ tables.size }} tables{% endif %}{% if views.size > 1 %} | {{ views.size }} views{% endif %}{% if functions.size > 1 %} | {{ functions.size }} functions{% endif %}