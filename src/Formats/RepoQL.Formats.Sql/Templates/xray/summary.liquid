{%- for table in tables -%}
TABLE {{ table.name }}: {% for col in table.columns limit:10 %}{{ col.name }}{% if col.type %} {{ col.type }}{% endif %}{% unless forloop.last %}, {% endunless %}{% endfor %}{% if table.columns.size > 10 %}, ...{% endif %}
{% endfor -%}
{%- for view in views -%}
VIEW {{ view.name }}{% if view.source_tables.size > 0 %}: → {% for src in view.source_tables limit:5 %}{{ src }}{% unless forloop.last %}, {% endunless %}{% endfor %}{% endif %}
{% endfor -%}
{%- for func in functions -%}
{{ func.type }} {{ func.name }}({% for p in func.params limit:5 %}{{ p.name }}{% if p.type %} {{ p.type }}{% endif %}{% if p.default %} = {{ p.default }}{% endif %}{% unless forloop.last %}, {% endunless %}{% endfor %}{% if func.params.size > 5 %}, ...{% endif %}){% if func.return_type %} → {{ func.return_type }}{% endif %}
{% endfor -%}