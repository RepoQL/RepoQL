{{ display_title }}{% if tags_or_headings and tags_or_headings.size > 0 %} | {% if headline_uses_tags %}{% for tag in tags_or_headings limit:8 %}#{{ tag | replace: ' ', '-' }}{% unless forloop.last %} {% endunless %}{% endfor %}{% else %}{% for heading in tags_or_headings limit:8 %}{{ heading }}{% unless forloop.last %}, {% endunless %}{% endfor %}{% endif %}{% endif %}
