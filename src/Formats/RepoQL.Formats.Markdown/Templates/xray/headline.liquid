{% if title %}{{ title }} — {% endif %}{{ file_name }} | {{ media_kind | default: media_base }} | {{ size_bytes | filesize }}{% if line_count %} | {{ line_count }} lines{% endif %}{% if images_count %} | images: {{ images_count }}{% endif %}{% if tables_count %} | tables: {{ tables_count }}{% endif %}{% if top_lang %} | lang: {{ top_lang }}{% endif %}{% if tags and tags.size > 0 %} | {% for tag in tags limit:2 %}#{{ tag }}{% unless forloop.last %} {% endunless %}{% endfor %}{% endif %}{% if topics and topics.size > 0 %} | topics: {% for t in topics limit:3 %}{{ t }}{% unless forloop.last %}, {% endunless %}{% endfor %}{% endif %}
