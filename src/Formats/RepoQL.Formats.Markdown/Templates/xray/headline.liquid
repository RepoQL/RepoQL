{{ display_title }} | {{ document_type_label | default: media_kind | default: media_base }} | {{ size_bytes | filesize }}, {{ line_count | default: 0 }} ln{% if tags_or_headings and tags_or_headings.size > 0 %} | {% if headline_uses_tags %}{% for tag in tags_or_headings limit:8 %}#{{ tag | replace: ' ', '-' }}{% unless forloop.last %} {% endunless %}{% endfor %}{% else %}{% for heading in tags_or_headings limit:8 %}{{ heading }}{% unless forloop.last %}, {% endunless %}{% endfor %}{% endif %}{% endif %}
