{{ file_name }} | xlsx.workbook | {{ size_bytes | filesize }} | {{ sheet_count }} sheet{% if sheet_count != 1 %}s{% endif %}: {{ sheet_names | join: ", " }} | {{ total_rows | number_with_delimiter }} rows{% if has_formulas %} | formulas{% endif %}{% if has_totals %} | totals{% endif %}{% if table_count > 0 %} | {{ table_count }} table{% if table_count != 1 %}s{% endif %}{% endif %}{% if chart_count > 0 %} | {{ chart_count }} chart{% if chart_count != 1 %}s{% endif %}{% endif %}
