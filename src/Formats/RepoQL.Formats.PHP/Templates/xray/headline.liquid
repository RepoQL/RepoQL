{{ file_name }} | {{ media_kind }} | ns:{{ namespace }}{% if classes.size > 0 %}{% for c in classes limit:2 %} | {{ c.name }}: {% for m in c.methods limit:5 %}{{ m.name }}{% unless forloop.last %}, {% endunless %}{% endfor %}{% if c.methods.size > 5 %}...{% endif %}{% endfor %}{% endif %}{% if functions.size > 0 %} | funcs: {% for f in functions limit:5 %}{{ f.name }}{% unless forloop.last %}, {% endunless %}{% endfor %}{% if functions.size > 5 %}...{% endif %}{% endif %}