{{ file_name }} | {{ media_kind | default: media_base }} | {{ size_human }}{% if stats.operation_counts and stats.operation_counts.size > 0 %} | {% for item in stats.operation_counts %}{{ item.label }}:{{ item.count }}{% unless forloop.last %}, {% endunless %}{% endfor %}{% endif %}{% if stats.fragment_count > 0 %} | fragments:{{ stats.fragment_count }}{% endif %}{% if stats.type_counts and stats.type_counts.size > 0 %} | {% for item in stats.type_counts %}{{ item.label }}:{{ item.count }}{% unless forloop.last %}, {% endunless %}{% endfor %}{% endif %}{% if stats.directive_count > 0 %} | directives:{{ stats.directive_count }}{% endif %}{% if stats.has_schema %} | schema{% endif %}
