{{ file_name }} | json | {{ size_bytes | filesize }}{% if token_count > 0 %}, {{ token_count | tokens }}{% endif %} | {{ shape }}{% if top_keys.size > 0 %} | {% for key in top_keys limit:8 %}{{ key }}{% unless forloop.last %}, {% endunless %}{% endfor %}{% if top_keys.size > 8 %}, ...{% endif %}{% endif %}
