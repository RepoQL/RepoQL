File: {{ file_name }}
Format: {{ media_kind }} (delimiter: {{ delimiter_name }})
Size: {{ size_bytes | filesize }}{% if token_count > 0 %} | {{ token_count | tokens }}{% endif %}
Rows: {{ row_count | number_with_delimiter }} | Columns: {{ column_count | number_with_delimiter }} | Header: {% if has_header %}yes{% else %}no{% endif %}
{% if type_counts and type_counts.size > 0 -%}
Types: {% for t in type_counts %}{{ t.type }}: {{ t.count }}{% unless forloop.last %}, {% endunless %}{% endfor %}
{% endif -%}
{% if total_estimated_tokens > 0 -%}
Estimated column tokens: {{ total_estimated_tokens | tokens }}
{% endif -%}
