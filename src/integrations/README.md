# integrations/

Bridges to external systems. Depends on contracts + data.

Put it here if it connects RepoQL to something outside — SARIF import, external MCP server discovery, repository import orchestration. Each integration adapts an external protocol or format into graph data.

Projects: `Sarif`, `Mcp.Client`, `Import`.
