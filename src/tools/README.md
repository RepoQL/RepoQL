# tools/

Build-time and dev-time utilities.

Put it here if it's a standalone tool used during development or build — snapshot generation, code generation, one-off scripts. Not part of the runtime.

Projects: `SnapshotGenerator`.
